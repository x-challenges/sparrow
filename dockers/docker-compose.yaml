name: sparrow

networks:
  backend:
    driver: bridge
    external: false
    name: backend

include:
  - path:
    - docker-compose.base.yaml
    - docker-compose.jupiter.yaml
    - docker-compose.tools.yaml
